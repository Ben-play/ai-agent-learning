# L06–L11 审核与优化（生成后自查）

用户要求"先审核刚出的这几节课再优化"。对 L06–L11 做了三轴审核（代码 / Quiz / 教学与链接），修掉了发现的问题。

## 审核结论
- **代码：全部正确。** 从 HTML 实际抽取每节课的代码块逐一核对，claimed-output 与真实运行一致，无笔误、无过时写法。（tenacity/orjson 本机未装，课程作外部库引用，此前已用 stdlib 等价验证过模式。）
- **交叉链接：全部有效。** 6 节课的内部课程链接目标都存在；外部 URL 除 realpython.com 因 Cloudflare 拦 curl 返回 403（浏览器正常）外，Python 官方文档 / tenacity / orjson / jsonlines 全部 200。
- **Quiz：发现真问题 → 已修。** 12 道题都恰好 1 个正确答案，但**有 5 道选项长度不均、且正确答案是最长项**——这是明显的答案泄露（教学技能明确要求选项等长、不通过格式泄露答案）。

## 修复
把 5 道有泄露倾向的 Quiz 重写选项，使**没有一道题的正确答案是唯一最长项**（复检：0 道仍泄露）：
- L07 q1（cls）、L07 q2（default_factory）、L08 q1（TypeError）、L10 q2（yield 后=清理）、L11 q1（f=deco(f)）、L11 q2（inspect 读签名）。
- 手法：统一选项句式、给干扰项补到相近长度、必要时加一个更长的技术性干扰项（如"触发 RuntimeError""调用 dir() 反射列出全部"），让"选最长的"策略失效。
- 另：L08 多态示意片段补了"（示意，完整可运行版见练习）"说明，避免学习者困惑于其中未定义的 CalcTool/args。

**Why:** 生成质量把关——选项长度泄露答案会让 Quiz 失去检索练习的价值（学习者靠长度猜而非真的回忆）。
**How to apply:** 以后写 Quiz 时，四个选项保持相近字数、句式平行，正确项绝不能是唯一最长/最详细的那个。这条已是隐性标准，值得写进课程审核清单。参见 [[0012-partb-lessons-authored]]。
