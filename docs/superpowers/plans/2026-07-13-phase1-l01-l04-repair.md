# Phase 1 L01–L04 Course Repair Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复第一阶段 L01–L04 中已复核的可执行错误、事实错误、练习契约冲突和初学者前置依赖问题，使四课可以按顺序学习并照着运行。

**Architecture:** 每课作为一个独立修复单元，只修改对应 HTML；先用最小复现脚本证明问题，再做克制修改，随后运行示例并检查 HTML/Quiz。课程顺序不重排：后课概念若提前出现，改成“提供现成骨架、当前只观察指定知识点”，不提前要求学员独立产出。

**Tech Stack:** 静态 HTML、共享课程 CSS/JS、Python 3.11+、uv、PowerShell/macOS/Linux shell、Python 标准库。

## Global Constraints

- 教学语言保持中文，语气和现有课程一致。
- 只修改 `lessons/0004`、`0005`、`0006`、`0008` 与 `NOTES.md`；不改 `code/`。
- 不新增第三方运行依赖；验证脚本优先使用 Python 标准库。
- Windows 命令明确标注 PowerShell；macOS/Linux 命令单独展示。
- 所有代码块必须明确执行环境：Shell、PowerShell 或 Python，不混装。
- 每个“应该输出”必须实际运行验证；平台命令无法跨平台执行时，以官方文档和本机可验证部分双重核对。
- 每课修改后检查 `div/pre/code/table/details/button` 标签平衡、Quiz ID 唯一、每题 4 个选项且恰好 1 个正确答案。
- 不提交、不推送；等用户审核完第一批后再统一提交。
- 提交前仍须扫描暂存区中的 Tavily Key 前缀（命令可用相邻引号拼接搜索词，避免计划文档自身触发扫描），但本计划执行阶段不暂存文件。

---

### Task 1: 修复 L01 环境搭建的跨平台可执行流程

**Files:**
- Modify: `lessons/0004-L01-dev-environment-setup.html:132-159`
- Modify: `lessons/0004-L01-dev-environment-setup.html:181-190`
- Modify: `lessons/0004-L01-dev-environment-setup.html:233-323`
- Modify: `lessons/0004-L01-dev-environment-setup.html:325-415`

**Interfaces:**
- Consumes: 当前 `uv init` 默认应用脚手架，以及课程要求的 Python 3.11+。
- Produces: 一条从版本检查 → 安装 uv → `uv init` → `uv run main.py` → `.env` 验证的可照抄流程。

- [ ] **Step 1: 保存修复前的可复现证据**

Run:

```bash
TMP=$(mktemp -d)
uv init "$TMP/demo"
ls -la "$TMP/demo"
test -d "$TMP/demo/.venv" && echo venv=yes || echo venv=no
(cd "$TMP/demo" && uv run main.py)
test -d "$TMP/demo/.venv" && echo venv=yes || echo venv=no
rm -rf "$TMP"
```

Expected:

```text
main.py 存在
uv init 后 venv=no
uv run main.py 后 venv=yes
```

- [ ] **Step 2: 重写 Python 版本检查为平台分流**

将统一的 `python3 --version` 改为：

```text
Windows PowerShell：py --version（优先）或 python --version
macOS / Linux：python3 --version
```

同时把“command not found”改成平台对应的错误解释，避免 Windows 已安装但没有 `python3` 时误判。

- [ ] **Step 3: 收敛 Python 安装说明**

保留 python.org 为全平台稳定路径；pyenv 只作为 macOS/Linux 可选路径，并明确：安装脚本结束后按终端提示配置 PATH、重开终端，再执行 `pyenv install`。不在本课展开 pyenv 的 shell 初始化脚本细节。

- [ ] **Step 4: 修正 uv 项目结构与虚拟环境创建时点**

把课程示例改为：

```bash
mkdir my-first-agent
cd my-first-agent
uv init
# 此时生成 main.py；.venv 尚未创建
uv run main.py
# 首次 uv run 会创建 .venv 并同步项目环境
```

目录树必须包含 `main.py`，不再出现 `hello.py`。

- [ ] **Step 5: 拆分 `.env` 创建命令**

加入两个独立代码块：

```powershell
New-Item .env -ItemType File
```

```bash
touch .env
```

注明也可直接在 VS Code 资源管理器中新建 `.env`。

- [ ] **Step 6: 修正 `--dev` 的解释与 Quiz**

在命令表后补充：

```text
--dev 表示把依赖记录进 dev 依赖组；uv sync / uv run 默认仍会同步 dev 组。生产环境要显式使用 --no-dev 等选项排除。
```

Quiz 正确项改为“记录到开发依赖组，生产同步时可显式排除”，并保持四个选项长度接近。

- [ ] **Step 7: 让 `.env` 成功条件可验证**

实战代码改为：

```python
api_key = os.getenv("OPENAI_API_KEY")
if not api_key:
    raise RuntimeError("未从 .env 读取到 OPENAI_API_KEY")
print(f"✅ .env 加载成功，Key 前缀：{api_key[:6]}...")
```

成功条件改为明确看到 `.env 加载成功`，而不是只数 3 个勾。

- [ ] **Step 8: 标记本课未教语法为“现成验证骨架”**

在 `BaseModel`、函数、类型注解和 `__name__` 示例前增加说明：这些不是本课考点；当前只需复制运行验证环境，它们分别会在 L02、L05、L13、L14 讲解。删除仅解释三元表达式和切片、却遗漏其余前置概念的不完整提示。

- [ ] **Step 9: 运行 L01 验证代码**

在临时目录按课程新版流程执行：

```bash
uv init agent-hello
cd agent-hello
uv add pydantic python-dotenv
printf 'OPENAI_API_KEY=test-course-key-123456\n' > .env
# 将课程实战 Python 代码保存为 main.py
uv run main.py
```

Expected: 显示项目初始化、`.env 加载成功`、Pydantic 正常和问候语；无异常。

- [ ] **Step 10: 检查 L01 HTML 和 Quiz**

Run:

```bash
PYTHONIOENCODING=utf-8 python scripts/check_lesson.py lessons/0004-L01-dev-environment-setup.html
```

若仓库没有 `scripts/check_lesson.py`，使用计划末尾的通用内联检查命令。Expected: 全部标签平衡，3 道 legacy Quiz 各 4 项且恰好一个 `true`。

---

### Task 2: 修复 L02 模块机制事实与练习契约

**Files:**
- Modify: `lessons/0005-L02-python-runtime-and-modules.html:52-90`
- Modify: `lessons/0005-L02-python-runtime-and-modules.html:163-303`
- Modify: `lessons/0005-L02-python-runtime-and-modules.html:327-399`
- Modify: `lessons/0005-L02-python-runtime-and-modules.html:411-521`
- Modify: `lessons/0005-L02-python-runtime-and-modules.html:524-625`

**Interfaces:**
- Consumes: L01 已创建的 `agent-hello` 项目；本课只考模块、包、import 和入口机制。
- Produces: 准确的 CPython 字节码缓存模型，以及一个不依赖 L05/L07/L13 才能独立完成的模块拆分练习。

- [ ] **Step 1: 写并运行 `.pyc` 边界复现**

```bash
TMP=$(mktemp -d)
printf 'print("main")\n' > "$TMP/mainmod.py"
printf 'x = 42\n' > "$TMP/imported.py"
(cd "$TMP" && python mainmod.py)
(cd "$TMP" && python -c 'import imported')
ls -R "$TMP"
rm -rf "$TMP"
```

Expected: `__pycache__/imported.*.pyc` 存在，`mainmod.*.pyc` 不存在。

- [ ] **Step 2: 修正字节码讲解**

正文改为：CPython 执行前会编译为字节码；直接从命令行运行的主模块通常只在内存中编译，不保存自己的 `.pyc`；被 import 的模块通常缓存到 `__pycache__`。知识图谱和面试答案同步。

- [ ] **Step 3: 区分项目目录与 Python 包名**

目录树改为：

```text
my-agent/        # 项目目录，可含连字符，不直接 import
└── agent/       # Python 包名，必须是合法标识符
    └── __init__.py
```

不得再把 `my-agent/` 标记为可 import 的包。

- [ ] **Step 4: 修正 import 查找分类**

把 `os/sys/json` 全称为内置模块的表述改为：先查 `sys.modules`；导入系统再通过 finder 处理 built-in/frozen/文件系统模块；对初学者可简化为“内置模块 + `sys.path` 上的标准库/第三方/项目模块”，并明确 `json` 是标准库包而非 built-in module。

- [ ] **Step 5: 修正 import 最佳实践**

删除“顶层只能用 import、函数内才用 from import”的错误绑定。改为：

```text
import module 与 from module import name 都可放顶层；是否延迟到函数内，取决于循环依赖、启动成本或可选依赖，而不是 import 语法形式。
```

- [ ] **Step 6: 修正相对导入示例**

在 `agent/tools.py` 的当前结构中只展示：

```python
from .schemas import Message
from .config import load_config
```

若需讲 `..`，增加一个真正有父包的 `project.agent.tools` 结构后再示范，不在当前顶层 `agent` 包中使用 `..config`。

- [ ] **Step 7: 修正循环导入因果模型**

正文改为：导入开始时模块对象先进入 `sys.modules`；再次导入通常拿到“部分初始化”的模块对象；只有访问尚未定义的名字等情况才报错。保留“提取公共模块”为首选根治方案，延迟导入为局部解法，`TYPE_CHECKING` 为类型注解专用方案。

- [ ] **Step 8: 给 `TYPE_CHECKING` 一个完整示例**

使用：

```python
from __future__ import annotations
from typing import TYPE_CHECKING

if TYPE_CHECKING:
    from agent.core import Agent

def bind(agent: Agent) -> None:
    ...
```

说明 future annotations 让注解延迟求值；该 import 只供类型检查器使用。

- [ ] **Step 9: 修正 `__init__.py` 执行次数**

改为“包首次导入时执行；普通重复 import 命中 `sys.modules`，不会重复执行；显式 reload 等特殊情况除外”。性能段同步为“首次 import agent 会加载 re-export 的模块”。

- [ ] **Step 10: 收敛 L02 练习到模块系统目标**

不要求学员从零编写类、`__init__`、`self`、`__repr__` 或复杂类型注解。参考答案直接提供 `schemas.py` 与 `core.py` 内容，任务改成：把给定代码放入正确模块、写 import、写 `__init__.py` re-export、运行 `main.py`、运行 pytest。

任务与答案统一导出：

```python
from .core import create_response
from .schemas import Message
__all__ = ["create_response", "Message"]
```

不再要求不存在的 `Agent`。

- [ ] **Step 11: 修正思考题方案编号**

把“优先想①的抽公共模块”改为“优先想②的抽公共模块”；同时说明延迟导入常见但不是架构根治。

- [ ] **Step 12: 运行模块练习验证**

在仓库现有 `code/phase1-python/part-a-basics/agent-hello` 执行：

```bash
uv run python main.py
uv run pytest -q
```

Expected: main 输出 Message；pytest 全部通过。

- [ ] **Step 13: 检查 L02 HTML/Quiz**

执行通用标签与 Quiz 检查。Expected: 标签平衡，Quiz ID 唯一，每题 4 项且 1 个 correct。

---

### Task 3: 补齐 L03 标量类型与 Python/JSON 边界

**Files:**
- Modify: `lessons/0006-L03-variables-types-data-structures.html:92-183`
- Modify: `lessons/0006-L03-variables-types-data-structures.html:350-407`
- Modify: `lessons/0006-L03-variables-types-data-structures.html:530-638`
- Modify: `lessons/0006-L03-variables-types-data-structures.html:605-757`

**Interfaces:**
- Consumes: 学员尚未系统学习 L04 的控制流和 L05 的函数参数。
- Produces: `int/float/str/bool` 的创建和区分；清晰区分 Python 对象与 JSON 文本；推导式和 `safe_get` 的前置依赖说明。

- [ ] **Step 1: 新增四种标量类型对照**

在可变/不可变前加入表格：

```text
int   42       整数
float 3.14     小数
str   "42"     文本
bool  True     真/假
```

点明 `42` 是 int，`"42"` 是 str；`True/False` 首字母大写；用 `type()`/`isinstance()` 检查。加入最小转换失败示例 `int("abc") -> ValueError`，但不提前展开异常处理。

- [ ] **Step 2: 将内存管理表述限定为 CPython**

改为“CPython 主要使用引用计数，并辅以循环垃圾回收；其他 Python 实现不保证对象在引用归零时立即释放”。缓存/驻留描述改为实现优化，不作为语言保证。

- [ ] **Step 3: 处理推导式的控制流前置**

保留普通循环对照，但在代码前明确：`for`/`if` 将在 L04 系统讲解；此处只按图读取三步，不把循环语法作为本课考核。Quiz 不考未教授的循环控制细节。

- [ ] **Step 4: 降低 `safe_get` 的函数参数前置负担**

保留朴素 `.get()` 链作为本课必学；通用 `safe_get(d, *keys)` 标为进阶阅读，明确 L05 后再要求独立实现。实战任务 5 改为“使用给定的 `safe_get` 并补测试路径”，不要求从零实现 `*keys` 函数。

- [ ] **Step 5: 区分 Python 对象与 JSON 文本**

把“模拟的 LLM JSON 响应”改为“模拟的、已经反序列化成 Python dict/list 的 LLM 响应”。补充：

```text
Python: None / True / False
JSON:   null / true / false
```

解释外层 `response` 已是 Python 对象，可直接索引；`function.arguments` 仍是 JSON 字符串，所以需要 `json.loads()`。将 `json.loads` 明确标为 L09 会系统讲，这里给出已写好的解析行。

- [ ] **Step 6: 修正实战验收任务**

任务 2/3 重点考 dict/list 与推导式；JSON 字符串解析代码作为提供的辅助函数，不把未学的 JSON API 作为独立考点。

- [ ] **Step 7: 运行 L03 实战答案**

抽取完整响应和参考答案到临时 Python 文件，运行并断言：

```python
assert response_id == "chatcmpl-abc123"
assert finish_reason == "tool_calls"
assert total_tokens == 165
assert [x["name"] for x in parsed_calls] == ["web_search", "calculator"]
assert safe_get(response, "choices", 99, default=None) is None
```

Expected: 全部断言通过。

- [ ] **Step 8: 检查 L03 HTML/Quiz 与时长**

执行通用检查，并按既有估时模型重新计算。若新增内容仍落在 40 分钟档则不改时长；若跨越 5 分钟取整边界，同步 `lesson-times.json`、course-bar 和大纲页。

---

### Task 4: 修复 L04 可运行性与函数前置倒置

**Files:**
- Modify: `lessons/0008-L04-control-flow-and-prompt.html:50-160`
- Modify: `lessons/0008-L04-control-flow-and-prompt.html:163-301`
- Modify: `lessons/0008-L04-control-flow-and-prompt.html:349-440`
- Modify: `lessons/0008-L04-control-flow-and-prompt.html:488-503`

**Interfaces:**
- Consumes: L03 的 list/dict/string 基础；函数定义与参数尚未在 L05 教授。
- Produces: 可运行的控制流示例；一个以“补全函数体”为主、而非从零设计函数签名的 Prompt 练习。

- [ ] **Step 1: 修复 Agent Loop 的未初始化变量**

代码改为：

```python
step = 0
done = False
MAX_STEPS = 10
while not done and step < MAX_STEPS:
    response = llm(messages)
    done = response.finish_reason == "stop"
    step += 1
```

同时将示例标为 OpenAI 风格简化字段；说明不同厂商字段和值不同，真实代码以 provider adapter 归一化。

- [ ] **Step 2: 修正 `length` 语义**

把 `length` 的动作从“上下文超限，需要压缩”改为“输出达到生成上限，当前回答可能被截断”。上下文窗口超限单独说明通常在请求阶段报错或由框架主动裁剪，不与输出 length 混淆。

- [ ] **Step 3: 修正独立示例的未定义变量**

在 f-string 示例中定义：

```python
MAX_RESULTS = 10
n = 5
```

将字符串判断里的 `response.lower()` 统一为已定义的 `resp.lower()`。

- [ ] **Step 4: 收敛 match-case 的能力表述**

把“这是 if/elif 做不到的”改为“if/elif 也能先判断再取字段；match-case 的优势是把结构匹配与变量绑定写在同一个声明式分支里”。

- [ ] **Step 5: 标注函数代码为现成容器**

在 Prompt 构造模式前增加：函数定义与参数在 L05 正式讲；本课只观察函数体中的分支、字符串和列表操作。移除不必要的类型注解，避免提前引入 L13：

```python
def build_prompt(question, context):
```

- [ ] **Step 6: 改造实战为补全函数体**

提供签名：

```python
def render_prompt(role, question, context="", tools=None, strict=False):
    # 在这里完成条件拼接
```

学员只需完成 `parts`、`if`、`append`、`join`，不要求自行设计默认参数。题目删除“f-string + 字典”中答案未使用字典的要求，改为“f-string + 条件分支 + 片段列表”。

- [ ] **Step 7: 避免生成器表达式前置**

将：

```python
context = "\n".join(f"{m['role']}: {m['content']}" for m in history)
```

改为已学列表推导式：

```python
lines = [f"{m['role']}: {m['content']}" for m in history]
context = "\n".join(lines)
```

- [ ] **Step 8: 运行 L04 可执行示例**

为 `llm()`/`run_tool()` 使用最小 stub，运行所有独立 Python 代码块；至少断言：

```python
assert action == "执行工具"
assert prompt == "还能再搜 5 条"
assert resp.startswith("Final")
assert "可用工具：search, calc" in build_system(...)
assert "问题：怎么退货？" in render_prompt(...)
```

Expected: 无 NameError，所有断言通过。

- [ ] **Step 9: 检查 L04 HTML/Quiz**

执行通用标签与 Quiz 检查；确认正确答案位置暂不与全课程答案位置重排混在本任务中，避免同时修改内容和评估分布。

---

### Task 5: 记录审核修复并执行第一批综合验证

**Files:**
- Modify: `NOTES.md`
- Verify: `lessons/0004-L01-dev-environment-setup.html`
- Verify: `lessons/0005-L02-python-runtime-and-modules.html`
- Verify: `lessons/0006-L03-variables-types-data-structures.html`
- Verify: `lessons/0008-L04-control-flow-and-prompt.html`

**Interfaces:**
- Consumes: Tasks 1–4 的修改结果。
- Produces: 可追溯的更新日志和第一批修复验证报告。

- [ ] **Step 1: 在 NOTES 更新日志记录第一批修复**

记录每课的真实问题、修法和运行证据；注明代理候选均经原文或实跑复核，不写入被排除的假阳性。

- [ ] **Step 2: 运行通用 HTML/Quiz 检查**

```bash
PYTHONIOENCODING=utf-8 python - <<'PY'
from pathlib import Path
import re
files = [
    Path('lessons/0004-L01-dev-environment-setup.html'),
    Path('lessons/0005-L02-python-runtime-and-modules.html'),
    Path('lessons/0006-L03-variables-types-data-structures.html'),
    Path('lessons/0008-L04-control-flow-and-prompt.html'),
]
for p in files:
    html = p.read_text(encoding='utf-8')
    for tag in ('div', 'pre', 'code', 'table', 'details', 'button'):
        opened = len(re.findall(rf'<{tag}(?:\s|>)', html))
        closed = len(re.findall(rf'</{tag}>', html))
        assert opened == closed, (p, tag, opened, closed)
    ids = re.findall(r'data-quiz="([^"]+)"', html)
    assert len(ids) == len(set(ids)), (p, ids)
    for block in re.findall(r'<div class="quiz".*?<div class="quiz-explanation">', html, re.S):
        assert block.count('<button') == 4, p
        assert block.count('data-answer="correct"') == 1, p
    print(p.name, 'OK')
PY
```

Expected: 四个文件均输出 `OK`。

- [ ] **Step 3: 检查内部链接**

运行只读链接解析，确认四课新增/保留的 L## 链接和 Q&A 锚点存在。Expected: 0 个坏链接。

- [ ] **Step 4: 审核工作区差异**

```bash
git diff --check
git diff --stat
git status --short
```

Expected: 只出现计划内四课、`NOTES.md`，以及本计划文件；无 `code/`、密钥或无关文件改动。

- [ ] **Step 5: 向用户报告但不提交**

报告每课修改、实际运行命令与结果、尚未处理的第二批课程；等待用户确认后再提交推送。

## Self-Review

- Spec coverage: L01 环境命令/uv/.env、L02 字节码/import/练习、L03 标量/JSON/前置、L04 可运行性/函数前置均有独立任务。
- Placeholder scan: 计划中没有 TBD/TODO；每个代码修改点给出目标文本或代码形状。
- Type consistency: `create_response`/`Message` 导出与现有 `code/agent-hello` 一致；L04 不再依赖 L13 类型注解。
- Scope discipline: 本批不处理 Quiz 全局位置重排、Phase 1 Capstone、Phase 2/3 修复，避免混合多个独立整改项目。
