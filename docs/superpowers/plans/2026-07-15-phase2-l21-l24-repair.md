# Phase 2 L21–L24 Course Repair Implementation Plan

**Goal:** 修复模型能力、Benchmark、成本与综合实战中的绝对化结论和伪通用 API，使 L21–L24 可验证、厂商边界清晰。

**Constraints:** 主代理完整通读；强声明查原始来源；代码离线实跑；Claude 只用官方 Anthropic SDK；不改 `code/`；不提交、不推送。

## Task 1 — L21 能力边界
- Base/Instruct 改为行为倾向与常见训练路线。
- 区分 SFT、RLHF/DPO 类偏好优化；微调与 RAG 不做绝对二选一。
- 区分直接文档输入与 RAG；API 默认无状态不等于应用不能记忆。
- 同步 Quiz、练习、速查卡、总结。

## Task 2 — L22 Benchmark 与部署边界
- 删除 SWE-bench“无刷分捷径”；加入 contamination、harness/scaffold、公开测试集影响。
- 区分 open source / open weights；合规不简化为 hosted=出境。
- 自托管成本拐点改为实测；合同 Agent 场景从硬约束开始。
- 加入评测集划分、rubric、重复运行和方差。

## Task 3 — L23 缓存与累计成本
- 修复 HTML；解释累计多轮成本常二次增长。
- 增加缓存盈亏平衡、模型相关最小前缀、准确 usage 字段。
- 第三方案例只作案例；Batch 50% 限定厂商；删除无副作用/永远优先。
- 重跑全部成本数学。

## Task 4 — L24 厂商适配器与离线评测
- 替换伪通用 OpenAI 风格请求为 provider adapter。
- Claude 使用官方 SDK + claude-opus-4-8；tiktoken 只估 OpenAI，Claude 用 count_tokens/usage。
- 离线 mock 必须覆盖一致、部分一致、发散分支。
- 加入 ground truth/rubric、重复运行与归一化选型；整条流水线离线跑通。

## Task 5 — 同步与回归
- 同步大纲、总览、导航、Q&A 标题、时长四处和总计。
- 平衡 Quiz 正确位置，检查 4 选项/1 正确。
- 全批 HTML、链接、代码、数学、JS、JSON 回归并独立审查。
